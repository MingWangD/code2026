# 测试环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code2026?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=GMT%2b8&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  # 测试时关闭定时任务执行
  task:
    scheduling:
      enabled: false

# MyBatis配置
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl  # 测试时关闭SQL日志

# 日志配置
logging:
  level:
    root: WARN
    com.example: INFO
    org.springframework: WARN